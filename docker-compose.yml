services:
  app:
    build: .
    container_name: asset_prod_app
    restart: always

    ports:
      - "8003:80"

    # TAMBAHKAN BARIS INI:
    volumes:
      - /srv/apps/asset-prod/.env:/var/www/html/.env

    networks:
      - shared

networks:
  shared:
    external: true
